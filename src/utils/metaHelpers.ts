// helper to parse meta payload (if needed). Kept simple for extensibility.

export function parseWebhookPayload(payload: any) {
  // returns normalized structure if you want to use it
  return payload;
}
